import React, { useEffect, useState } from 'react';
import MovieDetails from '../component/MovieDetails';

function MovieScreen(movie) {
  return (
    <div>
      <MovieDetails />
    </div>
  );
}

export default MovieScreen;
